server:
  address: ${SERVER_ADDRESS:0.0.0.0}
  port: 8082

spring:
  application:
    name: opc-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/opc_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  mail:
    host: smtp.qq.com
    port: 465
    username:
    password:
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true

opc:
  ai:
    settings-master-key: ${OPC_AI_SETTINGS_MASTER_KEY:}
    trusted-provider-origins: ${OPC_AI_TRUSTED_PROVIDER_ORIGINS:https://api.deepseek.com}
    agent:
      history-cursor-secret: ${OPC_ASSISTANT_CURSOR_HMAC_SECRET:}
      lease-seconds: ${OPC_AI_AGENT_LEASE_SECONDS:45}
  admin:
    session-hours: 12
  settings:
    encryption-secret: ${OPC_SETTINGS_SECRET:local-development-only}
    encryption-salt: ${OPC_SETTINGS_SALT:9f4c2d8a7b6e5f10}
  altcha:
    hmac-secret: ${ALTCHA_HMAC_SECRET:}
  auth:
    mail-enabled: false
    verification-code-minutes: 10
    resend-interval-seconds: 60
    session-days: 30

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
