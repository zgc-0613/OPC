server {
    listen 80;
    server_name admin.findopc.online;

    root /var/www/opc;

    location ^~ /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        default_type text/plain;
        return 200 "admin.findopc.online certificate bootstrap\n";
    }
}
